* text=auto

# Shell scripts must keep LF, including on Windows checkouts — the bot runs Linux
gradlew text eol=lf
*.sh    text eol=lf
*.py    text eol=lf

# Windows launchers keep CRLF
*.bat text eol=crlf

*.jar binary
*.png binary
